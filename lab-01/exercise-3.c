#include <stdio.h>

int main() {
	int x = 1;
	int y = 3;

	printf("Moje zmienne: x=%d, y=%d", x, y);

	// 4 Przy próbie utworzenia zmiennej o nazwie 1zmienna otrzymamy błąd, ponieważ zgodnie ze specyfikacją C nie można rozpoczynać nazw zmiennych cyfrą

	return 0;
}